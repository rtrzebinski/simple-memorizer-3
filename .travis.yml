language: php

php:
  - 7.4

before_script:
  - phpenv config-rm xdebug.ini
  - composer install --prefer-dist --optimize-autoloader --no-interaction
  - cp env-dev .env

script:
  - ./vendor/bin/phpunit
